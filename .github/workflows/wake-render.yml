name: Wake Render Backend
on:
  # 定时触发：每10分钟自动执行一次（UTC时间，对应北京时间每10分钟+8小时）
  schedule:
    - cron: '*/10 * * * *'
  # 保留手动触发功能
  workflow_dispatch:

jobs:
  wakeup:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render deploy hook
        run: curl -s ${{ secrets.RENDER_WAKE_HOOK }}
